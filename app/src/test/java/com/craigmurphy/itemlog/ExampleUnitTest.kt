package com.craigmurphy.itemlog

import org.junit.Assert.*
import org.junit.Test

// Basic local unit test example.
// Runs on the development machine instead of an Android device.
class ExampleUnitTest {

    // Simple example test.
    @Test
    fun addition_isCorrect() {

        // Confirms that 2 + 2 equals 4.
        assertEquals(4, 2 + 2)
    }
}